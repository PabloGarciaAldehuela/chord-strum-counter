package dev.pablocoding.contadorderasgueosdeacordes.ui.theme

import androidx.compose.ui.graphics.Color

// ── Luthier's Resonance / Tonewood Palette (Stitch Theme) ───────────────────
// Backgrounds & Woods
val EbonyDark            = Color(0xFF120E0D) // Deep ebony / dark soundhole canvas
val SurfaceDark          = Color(0xFF1E1211) // Rich mahogany card surface
val SurfaceContainerDark = Color(0xFF231F1D) // Slightly elevated container
val SurfaceVariantDark   = Color(0xFF2D1917) // Mahogany veneer / border track
val OiledMahogany        = Color(0xFF5C1D1A) // High-emphasis container / selected badge
val RosewoodLight        = Color(0xFF8A2E2A) // Cherry / red mahogany accent

// Primaries & Amber Glows
val AmberGold            = Color(0xFFFFB300) // Golden amber varnish (primary accent)
val AmberWarm            = Color(0xFFFFD79B) // Warm light amber (gradient start)
val AmberLight           = Color(0xFFFFE082) // Honey glaze highlight

// Accents & Materials
val IvoryBone            = Color(0xFFFBF4EB) // Crisp polished bone & ivory text
val SpruceCream          = Color(0xFFD9C7B6) // Aged spruce soundboard text
val PhosphorBronze       = Color(0xFFC4A482) // Phosphor bronze acoustic strings / inactive tracks
val StringSilver         = Color(0xFFE0E0E0) // Plain steel strings

// Functional / State Colors
val EmberCrimson         = Color(0xFFD32F2F) // Fiery crimson (stop / muted strings)
val VintageEmerald       = Color(0xFF43A047) // Vintage emerald green (personal best 🏆)
val ErrorRed             = EmberCrimson
val SuccessGreen         = VintageEmerald

// Light Theme equivalents (Warm Acoustic Studio)
val SurfaceLight         = Color(0xFFFFF8F0) // Unfinished spruce / blonde maple
val SurfaceVariantLight  = Color(0xFFF4EAE0) // Sanded cedar / warm linen
val RosewoodDarkAccent   = Color(0xFF4A1E1B) // Polished rosewood trim
val AmberWarmLight       = Color(0xFFE67C00) // Warm caramel / amber varnish
val OnSurfaceMuted       = Color(0xFF7A665A) // Walnut dust / secondary labels